const express = require("express"); const cors = require("cors"); const app = express(); app.use(cors()); app.use(express.json()); app.post("/api/generate-image", (req, res) => { const prompt = req.body.prompt; const random = Math.floor(Math.random() * 999999); console.log("Seed:", random); const encodedPrompt = encodeURIComponent(prompt); const base = "https://image.pollinations.ai/prompt/"; const image1 = base + encodedPrompt + "?width=1024&height=1024&seed=" + random + "&nologo=true"; const image2 = base + encodedPrompt + "?width=1024&height=1024&seed=" + (random + 1) + "&nologo=true"; const image3 = base + encodedPrompt + "?width=1024&height=1024&seed=" + (random + 2) + "&nologo=true"; res.json({ success: true, images: [image1, image2, image3] }); }); app.listen(3000, "0.0.0.0", () => { console.log("Backend running on http://0.0.0.0:3000"); });
